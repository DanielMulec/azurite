# Active Product Slice

Slice 7B, Request Correlation And Note Route Evidence, is the single active
product slice. Daniel approved implementation after its reconciliation and
adversarial reviews. Implementation, automated verification, and desktop
enabled/disabled Sentry QA passed on 2026-07-11.

The physical Pixel 6 acceptance session is deliberately deferred. Slice 7B
remains incomplete and active until the two open adversarial-review
save-integrity findings are repaired, the Back/sidebar finding is classified,
and Daniel and Codex then record the exact phone request/operation joins,
successful WYSIWYG save, persisted marker, trace and Replay proof, and
local-only backend boundary. The authoritative current evidence is
`docs/qa/slice-7b-request-correlation.md`.

After 7B closes, the next planned slice is Slice 7C, Markdown Fidelity And
Honest Dirty State.

If more than one file here claims `Proposed` or `Active` status, stop and resolve
the ownership conflict before implementation. Supporting architecture should be
linked from the active slice rather than copied into competing master plans.
