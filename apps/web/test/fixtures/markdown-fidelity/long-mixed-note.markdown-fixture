# Long mixed-format fidelity note

This sanitized fixture exercises exact Markdown authority across a note large enough to expose lifecycle, serializer, and browser-draft ordering mistakes.

## Section 1

Paragraph 1 combines *emphasis*, **strong text**, `inline code`, and a [sanitized link](https://example.com/section-1). It remains ordinary test data with no private note content.

- primary item 1
- secondary item 1
  - nested detail A
  - nested detail B

> Quoted observation 1: exact source stays authoritative until an accepted edit.

| Field | Value |
| --- | --- |
| section | 1 |
| mode | mixed |

```ts
const section1 = "fixture-1";
```

## Section 2

Paragraph 2 combines *emphasis*, **strong text**, `inline code`, and a [sanitized link](https://example.com/section-2). It remains ordinary test data with no private note content.

- primary item 2
- secondary item 2
  - nested detail A
  - nested detail B

> Quoted observation 2: exact source stays authoritative until an accepted edit.

| Field | Value |
| --- | --- |
| section | 2 |
| mode | mixed |

```ts
const section2 = "fixture-2";
```

## Section 3

Paragraph 3 combines *emphasis*, **strong text**, `inline code`, and a [sanitized link](https://example.com/section-3). It remains ordinary test data with no private note content.

- primary item 3
- secondary item 3
  - nested detail A
  - nested detail B

> Quoted observation 3: exact source stays authoritative until an accepted edit.

| Field | Value |
| --- | --- |
| section | 3 |
| mode | mixed |

```ts
const section3 = "fixture-3";
```

## Section 4

Paragraph 4 combines *emphasis*, **strong text**, `inline code`, and a [sanitized link](https://example.com/section-4). It remains ordinary test data with no private note content.

- primary item 4
- secondary item 4
  - nested detail A
  - nested detail B

> Quoted observation 4: exact source stays authoritative until an accepted edit.

| Field | Value |
| --- | --- |
| section | 4 |
| mode | mixed |

```ts
const section4 = "fixture-4";
```

## Section 5

Paragraph 5 combines *emphasis*, **strong text**, `inline code`, and a [sanitized link](https://example.com/section-5). It remains ordinary test data with no private note content.

- primary item 5
- secondary item 5
  - nested detail A
  - nested detail B

> Quoted observation 5: exact source stays authoritative until an accepted edit.

| Field | Value |
| --- | --- |
| section | 5 |
| mode | mixed |

```ts
const section5 = "fixture-5";
```

## Section 6

Paragraph 6 combines *emphasis*, **strong text**, `inline code`, and a [sanitized link](https://example.com/section-6). It remains ordinary test data with no private note content.

- primary item 6
- secondary item 6
  - nested detail A
  - nested detail B

> Quoted observation 6: exact source stays authoritative until an accepted edit.

| Field | Value |
| --- | --- |
| section | 6 |
| mode | mixed |

```ts
const section6 = "fixture-6";
```

## Section 7

Paragraph 7 combines *emphasis*, **strong text**, `inline code`, and a [sanitized link](https://example.com/section-7). It remains ordinary test data with no private note content.

- primary item 7
- secondary item 7
  - nested detail A
  - nested detail B

> Quoted observation 7: exact source stays authoritative until an accepted edit.

| Field | Value |
| --- | --- |
| section | 7 |
| mode | mixed |

```ts
const section7 = "fixture-7";
```

## Section 8

Paragraph 8 combines *emphasis*, **strong text**, `inline code`, and a [sanitized link](https://example.com/section-8). It remains ordinary test data with no private note content.

- primary item 8
- secondary item 8
  - nested detail A
  - nested detail B

> Quoted observation 8: exact source stays authoritative until an accepted edit.

| Field | Value |
| --- | --- |
| section | 8 |
| mode | mixed |

```ts
const section8 = "fixture-8";
```

## Section 9

Paragraph 9 combines *emphasis*, **strong text**, `inline code`, and a [sanitized link](https://example.com/section-9). It remains ordinary test data with no private note content.

- primary item 9
- secondary item 9
  - nested detail A
  - nested detail B

> Quoted observation 9: exact source stays authoritative until an accepted edit.

| Field | Value |
| --- | --- |
| section | 9 |
| mode | mixed |

```ts
const section9 = "fixture-9";
```

## Section 10

Paragraph 10 combines *emphasis*, **strong text**, `inline code`, and a [sanitized link](https://example.com/section-10). It remains ordinary test data with no private note content.

- primary item 10
- secondary item 10
  - nested detail A
  - nested detail B

> Quoted observation 10: exact source stays authoritative until an accepted edit.

| Field | Value |
| --- | --- |
| section | 10 |
| mode | mixed |

```ts
const section10 = "fixture-10";
```

## Section 11

Paragraph 11 combines *emphasis*, **strong text**, `inline code`, and a [sanitized link](https://example.com/section-11). It remains ordinary test data with no private note content.

- primary item 11
- secondary item 11
  - nested detail A
  - nested detail B

> Quoted observation 11: exact source stays authoritative until an accepted edit.

| Field | Value |
| --- | --- |
| section | 11 |
| mode | mixed |

```ts
const section11 = "fixture-11";
```

## Section 12

Paragraph 12 combines *emphasis*, **strong text**, `inline code`, and a [sanitized link](https://example.com/section-12). It remains ordinary test data with no private note content.

- primary item 12
- secondary item 12
  - nested detail A
  - nested detail B

> Quoted observation 12: exact source stays authoritative until an accepted edit.

| Field | Value |
| --- | --- |
| section | 12 |
| mode | mixed |

```ts
const section12 = "fixture-12";
```

## Section 13

Paragraph 13 combines *emphasis*, **strong text**, `inline code`, and a [sanitized link](https://example.com/section-13). It remains ordinary test data with no private note content.

- primary item 13
- secondary item 13
  - nested detail A
  - nested detail B

> Quoted observation 13: exact source stays authoritative until an accepted edit.

| Field | Value |
| --- | --- |
| section | 13 |
| mode | mixed |

```ts
const section13 = "fixture-13";
```

## Section 14

Paragraph 14 combines *emphasis*, **strong text**, `inline code`, and a [sanitized link](https://example.com/section-14). It remains ordinary test data with no private note content.

- primary item 14
- secondary item 14
  - nested detail A
  - nested detail B

> Quoted observation 14: exact source stays authoritative until an accepted edit.

| Field | Value |
| --- | --- |
| section | 14 |
| mode | mixed |

```ts
const section14 = "fixture-14";
```

## Section 15

Paragraph 15 combines *emphasis*, **strong text**, `inline code`, and a [sanitized link](https://example.com/section-15). It remains ordinary test data with no private note content.

- primary item 15
- secondary item 15
  - nested detail A
  - nested detail B

> Quoted observation 15: exact source stays authoritative until an accepted edit.

| Field | Value |
| --- | --- |
| section | 15 |
| mode | mixed |

```ts
const section15 = "fixture-15";
```

## Section 16

Paragraph 16 combines *emphasis*, **strong text**, `inline code`, and a [sanitized link](https://example.com/section-16). It remains ordinary test data with no private note content.

- primary item 16
- secondary item 16
  - nested detail A
  - nested detail B

> Quoted observation 16: exact source stays authoritative until an accepted edit.

| Field | Value |
| --- | --- |
| section | 16 |
| mode | mixed |

```ts
const section16 = "fixture-16";
```


## Closing checkpoint

The final section keeps its source spelling, blank lines, and one trailing newline.
<!-- fidelity-width-padding:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx -->
